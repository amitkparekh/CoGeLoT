# @package _global_

defaults:
  - override /transformer_decoder@model.policy.transformer_decoder: their_vima.yaml

wandb_config:
  architecture: encoder_decoder
  attn_layers: vima
  positional_encoding: absolute
  source: VIMA
