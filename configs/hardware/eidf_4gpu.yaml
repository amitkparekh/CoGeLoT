# @package _global_

defaults:
  - override /trainer: ddp
  - override /datamodule: from_hf

datamodule:
  batch_size: 16
  num_workers: 8

trainer:
  devices: 4
  num_nodes: 1
  accumulate_grad_batches: 2
